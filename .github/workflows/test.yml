name: Test Action

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run my TS action
        uses: ./ # run local action
        with:
          name: Yasitha
